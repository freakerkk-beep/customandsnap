[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: mọi đường dẫn không phải file tĩnh đều do React Router xử lý.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
